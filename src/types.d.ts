export interface ICardFormData {
  name: string;
  series: string;
  gen: string;
  event: string;
  version: number | "";
  gif: boolean;
  image: string;
}

export interface ICard {
  name: string;
  series: string;
  gen: string;
  event: string;
  version: number | "";
  gif: boolean;
  image: string;
}
